import { CardFamilyInfo } from './family';

export const defaultTrapAttribute = [
    'NORMAL',
    'CONTINUOUS',
    'COUNTER',
];

export const trapFamily: CardFamilyInfo = {
    name: 'trap',
    attribute: ['Trap'],
    subFamily: defaultTrapAttribute,
};